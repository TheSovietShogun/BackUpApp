<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="App_DX" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/azul_chingon</item>
        <item name="android:colorForeground">@color/azul_chingon</item>
        <item name="android:navigationBarColor">@color/azul_chingon</item>
        <item name="android:statusBarColor">@color/azul_chingon</item>
        <item name="colorPrimaryDark">@color/azul_chingon</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondaryInverse">@color/colorPrimary</item>
        <item name="colorPrimary">@color/azul_chingon</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.RoundedCornerMaterialButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/rounded_shape</item>
    </style>


</resources>
